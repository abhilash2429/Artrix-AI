# Removed: GoogleEmbeddingProvider.
# embed() lives on LLMProvider.embed() via GeminiProvider (app/services/llm/gemini.py).
# This file is intentionally empty — kept to preserve package structure.

